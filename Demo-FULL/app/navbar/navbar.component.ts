import { Component } from '@angular/core';

@Component({
    selector: 'navbar',
    moduleId: module.id,
    templateUrl: './navbar.html'
})

export class NavbarComponent {
    pageTitle: string = 'Demos';
};